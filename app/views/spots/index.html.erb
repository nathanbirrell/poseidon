<p id="notice"><%= notice %></p>

<div class="row">
  <% if user_signed_in? %>
    <div class="small-4 large-6 columns small-centered">
      <%= link_to '+ NEW SPOT', new_spot_path, class: 'btn' %>
    </div>
  <% end %>
</div>