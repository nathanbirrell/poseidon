<p id="notice"><%= notice %></p>

<div class="grid-x">
  <% if user_signed_in? %>
    <div class="small-4 large-6 cell small-centered">
      <%= link_to '+ NEW SPOT', new_spot_path, class: 'btn' %>
    </div>
  <% end %>
</div>